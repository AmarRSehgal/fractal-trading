# Verified against the interpreter these results were produced on:
# MacPorts python 3.13.12. Note that on this machine bare `python3` is an
# unrelated work virtualenv, so run everything as
#     env -u PYTHONPATH /opt/local/bin/python3.13 ...
#
# Pinned lower bounds are the versions the Round 4-5 numbers were produced
# with; see RESULTS.md for the exact set.

# core
numpy>=1.26            # 2.4.3
pandas>=2.1            # 2.3.3
scipy>=1.11            # 1.18.1
statsmodels>=0.14      # 0.15.0

# data
yfinance>=0.2          # 1.7.0
pyarrow>=14.0          # 23.0.1 -- parquet engine for .data_cache/

# models (script 07 only; falls back to sklearn's GBM if lightgbm is absent)
lightgbm>=4.0          # 4.7.0
scikit-learn>=1.3

# plots
matplotlib>=3.8        # 3.11.1

# tests
pytest>=8.0
